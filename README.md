# Фінансовий результат УЗЕ - Веб-моніторинг

Інтерактивний дашборд для візуалізації фінансових результатів учасників закупівель електроенергії (УЗЕ).

## Функціональність

- 📊 **Візуалізація прибутку** - відображення маржі по кожній станції з кольоровою індикацією (зелений > 0, червоний < 0)
- 📈 **Детальні графіки** - План (CM), Факт (FCM) та Прибуток на одному графіку
- 📅 **Вибір періоду** - перегляд даних за поточний або минулий місяць
- 🌙 **Темна/світла тема** - зручний перегляд у будь-який час доби
- 📱 **Адаптивний дизайн** - коректне відображення на мобільних пристроях
- 📥 **Автозавантаження даних** - дані автоматично підтягуються з папки data

## Структура проекту

```
financial_monitor/
├── index.html              # Головна HTML сторінка
├── style.css               # Стилі CSS
├── README.md               # Документація
│
├── js/                     # JavaScript модулі
│   ├── config.js           # Глобальні змінні та налаштування
│   ├── parser.js           # Парсинг CSV файлів
│   ├── data.js             # Обробка та фільтрація даних
│   ├── charts.js           # Створення графіків (міні та детальний)
│   ├── ui.js               # UI компоненти (картки, summary)
│   ├── modal.js            # Модальне вікно з детальним графіком
│   └── main.js             # Точка входу та ініціалізація
│
└── data/                   # Папка для CSV файлів
    ├── 2. Фінансовий результат УЗЕ поденно - CM_FCM.csv
    └── 2. Фінансовий результат УЗЕ поденно - Прибуток день.csv
```

## Опис JavaScript модулів

| Файл | Опис |
|------|------|
| `config.js` | Глобальні змінні (`stationsData`, `cmFcmData`, `profitData`) та шляхи до файлів даних |
| `parser.js` | Функції парсингу CSV: `parseCSV()`, `parseCSVLine()`, `parseNumber()`, `parsePercent()` |
| `data.js` | Обробка даних: `processData()`, `getFilteredData()`, `calculateStationSummary()` |
| `charts.js` | Графіки Chart.js: `createMiniChart()`, `createDetailChart()`, `updateAllCharts()` |
| `ui.js` | UI елементи: `renderDashboard()`, `createStationCard()`, `updateSummary()`, `updateModalStats()` |
| `modal.js` | Модальне вікно: `initModalHandlers()`, `openDetailModal()`, `closeDetailModal()` |
| `main.js` | Ініціалізація: `loadDataFromFiles()`, `initThemeToggle()`, `initPeriodSelector()` |

## Формат CSV файлів

### CM_FCM.csv
Містить дані плану (CM) та факту (FCM) для кожної станції:
- Перший стовпець: дата (DD.MM.YYYY)
- Другий стовпець: номер місяця
- Далі пари стовпців CM, FCM для кожної станції

### Прибуток день.csv
Містить дані прибутку (маржі):
- Перший стовпець: дата (DD.MM.YYYY)
- Другий стовпець: номер місяця
- Далі пари стовпців: Маржа (тис. грн), Маржа (%)

## Використання

### Локальний запуск
```bash
cd financial_monitor
python -m http.server 8080
# Відкрити http://localhost:8080
```

### GitHub Pages
1. Завантажте папку `financial_monitor` на GitHub
2. Увімкніть GitHub Pages в налаштуваннях репозиторію
3. Сайт автоматично підвантажуватиме CSV файли з папки `data`

## Вибір періоду

- **Поточний місяць** - дані з 1-го числа до останнього дня з ненульовими даними
- **Минулий місяць** - всі дані за попередній місяць

## Станції

1. ТОВ "ЗОРЯНСЬКА БЕСС"
2. ТОВ «ОЛЕКСАНДРІЙСЬКА БЕСС»
3. ТОВ «СМОЛІНСЬКА БЕСС»
4. ТОВ «НОВОМИРГОРОДСЬКА БЕСС»
5. ТОВ «ДРАБІВСЬКА БЕСС»
6. ТОВ «НЕСС ПАУЕР»
7. ТОВ «СОЛІПАУЕР ТРЕЙДІНГ» *(без плану)*
8. ТОВ "ЗНАМЕНСЬКА БЕСС"

## Технології

- HTML5 / CSS3
- JavaScript (ES6+)
- [Chart.js](https://www.chartjs.org/) - бібліотека графіків
- [Chart.js Zoom Plugin](https://www.chartjs.org/chartjs-plugin-zoom/) - масштабування графіків
- [date-fns Adapter](https://date-fns.org/) - робота з датами
- [Font Awesome](https://fontawesome.com/) - іконки

---

Створено відділом агрегації KNESS Energy
